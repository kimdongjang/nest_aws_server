import Layout from "../../components/Layout";


const Content = (props:any) => {
    console.log(props)
    return (
        <div>
            {/* <h1>{props.url.title}</h1> */}
        </div>
    )
}

export default () => (
    <Content/>
)